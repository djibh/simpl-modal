{
  "name": "smpl-modal",
  "version": "0.2.1",
  "description": "A simple modal you can use to pass component as props, select a theme (dark / light / neutral) and if it should be animated or not.",
  "main": "index.ts",
  "Module": "index.ts",
  "Files": ["dist"],
  "author": "Julien Riera",
  "license": "ISC",
  "devDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
